import VoiceSession from '@/Components/Jarvis/VoiceSession';

/** @deprecated M23: VoiceSession is the Workspace Voice runtime client. */
export default function VoiceModePlaceholder(props) {
    return <VoiceSession {...props} />;
}
